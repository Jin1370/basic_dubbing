# 2026-04-13 — P1: 서버 CORS 확인 + SQL 마이그레이션 스크립트 생성

## BACKLOG 항목
- [P1] server/main.py — CORS origin을 환경변수로 분리
- [P1] server/models/database.py — Supabase 테이블 생성 SQL 스크립트 작성
- [P1] components/ 전체 lint 에러 수정

## 판단
### CORS 환경변수 분리
- `server/main.py:40` 에서 이미 `CORS_ORIGINS` 환경변수 사용 중
- JSON 배열 파싱 + 폴백까지 구현되어 있음
- 추가 작업 불필요, 완료로 마킹

### SQL 마이그레이션 스크립트
- `database.py`의 주석에 SQL이 있었으나 실행 가능한 .sql 파일이 없었음
- `server/migrations/001_init.sql` 생성
- 주석 SQL 대비 추가: `updated_at` 자동 갱신 트리거, `IF NOT EXISTS` 가드

### lint 에러 수정 — [blocked]
- 프로젝트에 ESLint 설정이 없음 (.eslintrc 없음, package.json에 lint 스크립트 없음)
- lint 환경 구축 자체가 별도 작업이므로 blocked 처리

## 변경 파일
- `server/migrations/001_init.sql` — 신규 생성

## 테스트 결과
- `npx tsc --noEmit` — 성공

## 다음 루프 주의사항
- P1 잔여 항목: lint 에러 수정 (blocked — ESLint 미설정)
- P2로 이동 가능
